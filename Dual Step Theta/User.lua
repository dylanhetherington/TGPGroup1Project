

User = { health,
         keyPressed,
         score }
       
function User.CheckPlayerHealth(health)
end

function User.CheckKeyPressed(keyPressed)
  
end

function User.ScoreManager(score)
  
end

function User.update(dt)
end
